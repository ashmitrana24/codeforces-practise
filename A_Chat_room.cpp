#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using vi = vector<int>;
using vll = vector<long long>;

void solve() {
    string s;cin>>s;
    int n=s.size();
    string ori="hello";
    int i=0,j=0;
    while(i<ori.size()){

        if(ori[i]==)




    }
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();
    return 0;
}